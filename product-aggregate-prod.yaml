spring:
  application:
    name: product-aggregate
  main:
    banner-mode: off

server:
  port: 8882

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7777/eureka/
  instance:
    preferIpAddress: true